export enum GitStatus {
	CLEAN = "CLEAN",
	MODIFIED = "MODIFIED",
}

export enum OpenProjectMode {
	FILE_EXPLORER,
	IDE,
}

export enum TitleBarAction {
	CLOSE_WINDOW,
	MINIMIZE_WINDOW,
	MAXIMIZE_WINDOW,
}
